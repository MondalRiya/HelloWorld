import { MyPipes\searchFilterPipe } from './my-pipes\search-filter.pipe';

describe('MyPipes\searchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MyPipes\searchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
